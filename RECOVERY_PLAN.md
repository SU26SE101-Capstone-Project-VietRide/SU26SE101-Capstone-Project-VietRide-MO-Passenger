# VietRide Passenger Recovery Tracker

> Last updated: 2026-08-01
> Mobile baseline: `main@091a5b4`
> UI/product recovery reference: `28cc5b7`
> Backend source of truth: `main@cd355fff` / `v1.53.0`
> Rule: backend source code and DTOs win over documentation and older mobile mocks.

## Status model

| Status | Meaning |
| --- | --- |
| `TODO` | Not started. |
| `IN_PROGRESS` | Implementation exists, but at least one acceptance gate remains. |
| `VERIFIED` | The row's complete acceptance criteria passed with recorded evidence. |
| `BLOCKED_BE` | The product capability is preserved, but BE has no safe contract. |
| `FAILED_VERIFY` | A required verification gate was attempted and did not pass. |

`DONE` is not used. `BLOCKED_ENV` is a verification-scope result, not a task status.

## Verification scope

| Scope | Result | Evidence / boundary |
| --- | --- | --- |
| `UNIT_STATIC` | `PASS` | 2026-08-01 current notification, Shuttle tracking and v1.53 History payment-resume sources pass full TypeScript, full zero-warning ESLint, `check:i18n` with 1,253 EN/VI keys in parity, full diff check, Expo SDK dependency/config checks and a 5,211-module Android Hermes export. Jest was intentionally not run per owner request; earlier full Jest evidence does not cover this batch. |
| `NATIVE_ANDROID_BUILD` | `PARTIAL_PASS` | The previously installed Expo 54/RN 0.81 development client predates RNFirebase Messaging and Notifee. For this batch, native autolinking and the merged Android manifest are verified, while a clean cache build compiled for 20 minutes without reaching APK packaging. A new short-path/EAS development-client build with an injected `google-services.json` is required before push can run on-device. |
| `DEVICE_ANDROID` | `PARTIAL_PASS` | API 36 emulator evidence remains valid for Login/Home/Chatbot/History, branded cold launch and exact payment deep-link resolution. That installed APK predates RNFirebase Messaging/Notifee, so it is not evidence for system-tray push or the new daily reminder. Live Shuttle GPS/ETA, History payment resume, push delivery/tap, Wallet return, RAG and Digital Ticket remain unverified on the rebuilt client. |
| `LIVE_BE` | `PARTIAL_PASS` | Authenticated Android startup received 200 responses from `/locations`, `/users/me`, `/promotions` and `/wallet`. No safe booking/payment mutation sandbox was available, so no real Shuttle booking, top-up, tracking session or RAG stream was fabricated. |
| `DEVICE_IOS` | `BLOCKED_ENV` | Windows host; no EAS token, project ID, `eas.json`, or local iOS simulator was available. |
| `GOOGLE_MAPS_CLOUD` | `BLOCKED_ENV / BLOCKED_TERMS` | Google Cloud console IAM/key state could not be inspected and real Android/iOS keys remain unset. The official Prohibited Territories list currently includes Vietnam, so production Maps integration is now fail-closed unless Google and qualified legal review document an eligible distribution/service footprint. Package/bundle/SHA-restricted credentials, key-enabled native builds and authenticated physical-device map smoke remain required after that gate is cleared. |
| `EXPO_DOCTOR` | `PARTIAL_PASS` | 2026-08-01 dependency versions match Expo SDK 54 and 16/17 checks pass. React Native Directory metadata still marks `react-native-nitro-google-signin` as not New-Architecture-tested and `@notifee/react-native` as unmaintained. The warnings are not hidden with an exclusion; Android native manifest processing succeeds, while rebuilt physical-device runtime smoke remains the release gate. The native-field sync check stays intentionally disabled because this repository tracks native Android. |
| `ANDROID_RELEASE_PERF` | `PARTIAL_PASS` | 2026-07-18 - the physically short Windows build path produced a verified, installable universal internal APK. Production/Play and frame-performance certification remain open because the APK uses the tracked debug signer, the Maps value is `TEST_KEY_FOR_LOCAL_BUILD`, the passenger App Link/domain is not deployable, and no release frame trace was recorded. |

## Canonical work tracker

| ID | Priority | Capability | Source of truth | Status | Dependencies | Acceptance criteria | Evidence | Last updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R0 | P0 | Recovery governance | Mobile `091a5b4`, recovery ref `28cc5b7`, BE `main@cd355fff` / `v1.53.0` | `VERIFIED` | None | One canonical tracker; disposition for every removed capability; no silent capability deletion; current Mobile/BE revisions pinned before every new audit | 2026-08-01 source audit re-pinned Mobile and BE before implementing the additive v1.53 History payment-resume contract; no product scope was retired | 2026-08-01 |
| M1.1 | P0 | Typed navigation | Registered navigators and discriminated params | `VERIFIED` | M6, M5 | Every declared route has a registered screen and every caller supplies valid params | `navigationRegistry.contract.test.ts`; TypeScript; full Jest; Android Home/Chatbot/History navigation smoke | 2026-07-15 |
| M1.2 | P0 | Booking network DTO and payment lifecycle | BE `origin/main@0a1bd9f` Booking controller/request/result/status DTOs | `IN_PROGRESS` | Existing booking store, authenticated VNPay sandbox | Exact one-way/round-trip bodies; `{seatNumber}` wire seats only; contact local; header idempotency; WALLET confirmation; VNPay foreground reconciliation for every booking leg; live checkout confirmation | `bookingApi.test.ts`, `useBookingStore.create.test.ts`, `bookingCompletion.test.ts`, `bookingPayment.test.ts`, `useBookingPaymentReconciliation.test.tsx`; 2026-07-17 full static gates pass; live create/payment flow not run | 2026-07-17 |
| M1.3 | P0 | Demo-mode boundary | Expo build env and missing-BE capabilities | `VERIFIED` | Env typing | Dev defaults only under `__DEV__`; staging requires explicit opt-in; production always false | `demoMode.test.ts`; production Hermes bundle built with `DEMO_MODE=true` still rendered fail-closed guest/history boundary on Android | 2026-07-15 |
| M1.4 | P0 | Per-leg Shuttle booking request | BE `origin/main@0a1bd9f` create handlers, Shuttle DTOs, Station and Trip detail DTOs | `IN_PROGRESS` | M1.2, station/trip detail, foreground location, safe live booking sandbox | Exact active origin Station only; fetched trip must be `SCHEDULED` with valid departure; strict T-30 cutoff; outbound/return drafts isolated; coordinates memory-only; local `stationId` never enters wire DTO; pending-assignment wording; live one-way/round-trip confirmation | `shuttle.test.ts`, `useBookingStore.create.test.ts`, `stationApi.test.ts`, `useStationDetail.test.tsx`, `deviceLocation.test.ts`, Shuttle component tests; 35 focused tests and full 66-suite gate pass; Android bundle/Home smoke pass; live Shuttle submit not run | 2026-07-17 |
| M1.5 | P1 | Shuttle intent/assignment visibility | BE `origin/main@0a1bd9f` booking status plus notification/tracking contracts | `BLOCKED_BE` | Passenger booking-linked Shuttle read contract | Never infer Shuttle status from create/status responses; surface assignment only from authenticated server data; no History fabrication | Create result does not echo Shuttle; `GET /bookings/{id}` returns booking status only; notifications exist only after `SHUTTLE_ASSIGNED`/`SHUTTLE_UNFULFILLED`, so pending intent cannot be reconstructed | 2026-07-17 |
| M2.1 | P1 | Popular routes | Live location catalog | `VERIFIED` | Location catalog | No guessed IDs, fare or duration; unresolved shortcuts hidden | Resolver tests; Android Home rendered location-only route cards without fake price/duration; full static gates pass | 2026-07-15 |
| M2.2 | P1 | Recent searches | AsyncStorage local adapter | `IN_PROGRESS` | Expo-compatible AsyncStorage | User/guest namespace, schema migration, corrupt-data reset, dedupe/max 8, device persistence | Adapter/hook tests and Home empty state pass; end-to-end search persistence not run against a live catalog | 2026-07-15 |
| M2.3 | P1 | Home wallet card | Payment Wallet API | `IN_PROGRESS` | M3.1 | Real loading/error/balance states and typed Wallet navigation | Static implementation/gates pass; authenticated device flow not run | 2026-07-15 |
| M3.1 | P0 | Wallet ledger | BE `WalletController` | `IN_PROGRESS` | Auth session | Exact `/wallet` and paginated `/wallet/transactions`; user cache isolation; 100+ rows on device | Contract, isolation and 125-unique-row pagination tests pass; live authenticated ledger/device list not run | 2026-07-15 |
| M3.2 | P0 | VNPay top-up | BE `WalletController` | `IN_PROGRESS` | M3.1, redirect validator | Exact `/wallet/top-up`; single flight; ambiguous timeout safety; foreground refresh once; no fake success | Coordinator/AppState/double-tap/scoped-refresh tests pass; real redirect/return not run | 2026-07-15 |
| M3.3 | P1 | Unsupported financial surfaces | Current BE controller inventory | `IN_PROGRESS` | Profile navigation | Withdraw/Saved Payments/Add Card remain labelled, fail-closed shells; no PAN/CVV collection | Capability/navigation tests pass; authenticated device reachability not run | 2026-07-15 |
| M3.4 | P0 | Mobile payment-return custom deep link | Mobile payment handler; BE VNPay return plus authenticated Booking/Parcel/History state | `IN_PROGRESS` | M1.2, M3.2, M6.5, signed Android device build | Accept only exact `vietride://payments/return` on cold/warm launch; reject ambiguous/spoofed URLs; never trust, persist or display gateway query data as success; mark Booking, Parcel and user-scoped History caches stale-only, let the initiating History screen own its exact foreground refetch, and leave Wallet reconciliation to its foreground single-flight owner | Existing tests/device evidence cover exact parsing, hostile URL rejection and installed-APK resolution. v1.53 History reconciliation wiring passes TypeScript/full ESLint but was not Jest/device verified per owner request | 2026-08-01 |
| M3.5 | P0 | HTTPS VNPay return bridge and verified App Link | BE `410614bdaa5f911edd6d33f86a342e163901b38c` / `v1.34.2`; passenger domain and production certificate ownership | `BLOCKED_BE` | `app.vietride.online` DNS, public HTTPS return route, passenger `assetlinks.json`, persistent production signing certificate | Keep `VNPAY_RETURN_URL=https://app.vietride.online/payments/return`; page/bridge may wake the app at the fixed mobile URI but must not forward or present query parameters as payment success; publish raw-200 passenger App-Link ownership for `com.vietride.passenger`; IPN/authenticated APIs remain authoritative | `app.vietride.online` currently has no DNS record or HTTPS return bridge. The apex-domain `assetlinks.json` currently identifies the driver package with a debug fingerprint, not the passenger package/domain; the passenger production certificate does not exist yet. Custom URI fallback is verified, but production HTTPS App Link remains BE/domain-blocked | 2026-07-18 |
| M4.1 | P1 | Booking-voucher promotions | Existing `bookingApi.getPromotions` | `IN_PROGRESS` | Booking API | Reuse API/query keys; no second client; loading/error/empty states | Hook/component tests and duplicate-owner audit pass; live response not run | 2026-07-15 |
| M4.2 | P1 | Received parcels | BE `origin/main@d3dc7716` create and `GET /parcels/received` handlers | `BLOCKED_BE` | Safe recipient account linking | Keep the existing endpoint/hook/UI owner; a created parcel must resolve to the intended Passenger before the received list can populate | Public create currently persists `RecipientUserId=null`, while received queries filter by that field. UI remains preserved and status-compatible, but Mobile cannot infer identity from contact PII | 2026-07-28 |
| M4.3 | P1 | Editorial news and product promos | No current passenger BE contract | `BLOCKED_BE` | News/editorial endpoint and DTO | Preserve capability; do not restore invented copy, Picsum media or fake campaign data | BE controller inventory has voucher promotions only; this is distinct from M4.1 | 2026-07-15 |
| M4.4 | P1 | Referral rewards | No current passenger BE contract | `BLOCKED_BE` | Referral/reward endpoint and DTO | Preserve capability; do not invent invite rewards or coin balances | Recovery source contained hard-coded reward copy; no supporting BE contract exists | 2026-07-15 |
| M5 | P1 | Ticket and Parcel live trip tracking with native map | BE `main@cd355fff` / `v1.53.0`; passenger wire contract retained from `fcccf454`, producer dedupe `99e2884d` | `IN_PROGRESS` | Auth, valid `tripId`, Parcel `dropoffStopId`, regional/Terms eligibility, restricted Google Maps keys, live tracking session | Socket.IO joins/rejoins the trip using bearer auth without query/log leakage; validates and scopes live GPS/ETA/status events; REST latest is bootstrap/fallback only; trail/ETA/status obey focus/network/AppState/terminal lifecycles; Ticket and Parcel reuse one panel; native map safely renders/caps trail and stop markers; camera follows without overriding user pan/pinch; Google attribution stays visible; consume additive destination/stop arrival fields without inventing lifecycle state; null/403/404 and missing IDs fail clearly | Current v1.53 audit confirms no Passenger tracking DTO/event change; the new producer-side dedupe needs no Mobile payload adjustment. One existing tracking panel/API/query owner remains reused. Map viewport, gesture-follow policy, bounded camera animation, static markers, contrast-safe polyline and fail-closed native key config remain intact. Static gates pass; regional eligibility, real Cloud credentials, key-enabled native compile and authenticated physical-device smoke remain open | 2026-08-01 |
| M5.1 | P1 | Passenger Shuttle live tracking | BE `main@cd355fff` / `v1.53.0`; tracking contract retained from `fcccf454`, latest producer dedupe `99e2884d`, plus `SHUTTLE_ASSIGNED` notification | `IN_PROGRESS` | M5; authenticated assignment notification containing exact `shuttleTripId`; native Maps config | From Notification Detail, an explicit user CTA opens a discriminated Shuttle tracking route only for allow-listed `SHUTTLE_ASSIGNED` data with a valid UUID. Reuse the existing map/panel/query/socket owner; consume nullable `/tracking/shuttle-trips/{id}/latest|eta`, `joinShuttleTracking`, `shuttle:gps:update` and `shuttle:eta:update`; stop work when unfocused, offline or backgrounded; never execute notification `deepLink`, infer assignment, request trail/status, or substitute the main trip ID | Implemented strict Shuttle DTO normalization (`heading` -> canonical map heading), isolated user/source query keys, REST fallback, scoped realtime events, null/403/404 states, Shuttle map presentation and safe notification CTA. Current BE changes only deduplicate producer GPS writes; no passenger wire change was required. TypeScript, full zero-warning ESLint and EN/VI parity pass; authenticated physical-device GPS/ETA/permission smoke remains open | 2026-08-01 |
| M6 | P0 | Unified Passenger History data/query/navigation foundation | BE `main@cd355fff` / `v1.53.0` `GET /v1/passenger/history` facade | `IN_PROGRESS` | M1.1, authenticated passenger session | Required `type=TICKET\|PARCEL`; server filters; cancellable infinite pagination; user-isolated cache; lazy inactive tab; sent-Parcel boundary; Ticket snapshot and Parcel UUID detail handoffs; strict nullable trusted payment redirect; no fixture fallback or per-row network request | One facade/API/query owner remains reused. v1.53 `paymentRedirectUrl` is parsed into the discriminated network model, defaults only for rolling older responses, and is removed from Ticket navigation state; authenticated pagination/detail/payment flow remains unverified | 2026-08-01 |
| M6.1 | P0 | History list information architecture and shared semantic presentation | Mobile `afb7ea8` History, Booking/Parcel presentation owners, shared i18n/theme primitives | `IN_PROGRESS` | M6 | Route/recipient is primary and operational codes are secondary/selectable; sent Parcel copy is sender-facing; no raw enum, size, delivery value, technical `ETA` prefix or hard-coded English survives in List or existing Detail screens. Booking owns Booking/Ticket status and trackability presentation; Parcel owns status/size/delivery presentation while existing payment/tracking policy utilities remain authoritative; History only composes them and one shared status-chip primitive. Runtime parsing accepts bounded enum-like future statuses, but only known allow-listed statuses can enable Track/Payment/actions; unknowns render a localized neutral state and fail closed. Preserve user-scoped FlashList pagination, memo rows, stable callbacks, no list images or row queries, sibling Pressables at least 44 px, and responsive route/meta layout for small screens and large text | Implemented bounded future-status parsing, domain-owned Ticket/Parcel presenters, a shared status chip and localized route-first FlashList rows; known status actions remain allow-listed. TypeScript, scoped ESLint and Android Metro export pass; real device/large-font verification remains open | 2026-07-30 |
| M6.2 | P0 | Ticket History detail and boarding-ready QR presentation | Ticket branch of `GET /v1/passenger/history`, existing `DigitalTicketScreen` and `buildPassengerHistoryTicketViewModel` | `IN_PROGRESS` | M6, M6.1; M5 only for eligible Track; M6.5 for payment resume | Every normal History route requires the selected server snapshot and never reads checkout state or creates another detail API. Reuse the existing view-model owner; localize departure date/time, direction, route, amount and per-ticket lifecycle summaries without inventing bus, stop address/ID, payment method, Shuttle state or arrival data. Preserve each `seatNumber <-> ticketCode` pair and render one high-contrast, scannable QR per live ticket using the exact server `ticketCode` (`VT-...`), with a selectable text fallback; never encode booking code, `ticketId`, `passengerRecordId` or PII. Multiple tickets must be individually selectable, not joined into one code string. The M6.5 payment CTA remains a sibling List action and its signed URL never enters Detail navigation; narrow/large-text layout and all accessibility labels remain usable | Reused the History snapshot and Ticket view-model; removed fallback detail fetch for normal History routes. Each ticket now retains seat/code/status and renders its own QR plus selectable code; per-ticket `USED` is localized. TypeScript, scoped ESLint and Android Metro export pass; physical scanner/device flow remains open | 2026-08-01 |
| M6.3 | P0 | Sent Parcel History detail, role-safe actions and operational QR | Sent Parcel facade plus `GET /v1/parcels/{parcelId}`, existing `ParcelDetailScreen`/query/payment/tracking owners | `IN_PROGRESS` | M6, M6.1, M5; M6.5 for payment resume | History navigation passes only `parcelId` plus presentation origin, never signed URLs or image URIs. Reuse the existing detail query and derive sender/recipient role from authenticated DTO ownership fields, not navigation flags; sender-only payment/drop-off controls stay hidden for recipient viewers while authorized tracking remains contract-driven. Format status, size and delivery through Parcel-owned vocabulary; event times use only their exact BE timestamp fields. Render a real QR from exact `parcelCode` only when the existing Parcel lifecycle policy permits, not a decorative QR icon or parcel UUID. Keep server-owned `expo-image` evidence in Detail only; M6.5 resume opens the existing History redirect directly and never calls Start-payment merely to recover a URL | Existing detail query/photo gallery remain reused. Added role-derived payment UI, Parcel-owned status/size/delivery presentation and a real QR only for the existing active-code policy. TypeScript, scoped ESLint and Android Metro export pass; sender/recipient physical-device flows remain open | 2026-08-01 |
| M6.4a | P1 | Ticket-use summary available from current History contract | Current `tickets[].status` projection and Booking-owned presentation vocabulary | `IN_PROGRESS` | M6.1, M6.2 | Per-ticket `USED` is presented as a localized used/scanned state and can aggregate as `1/2 tickets used`; it never claims a live Boarding window, timestamp or stop and never exposes internal passenger identifiers | Each ticket QR card now carries its localized per-ticket lifecycle state, including `USED`; device scan/visual verification remains open | 2026-07-30 |
| M6.4b | P1 | Live boarding window and per-ticket boarding facts in History/detail | Additive authenticated Booking/History projection plus BE1 | `BLOCKED_BE` | BE1, explicit passenger-facing projection | Model cardinality correctly: `tripStatus` at the History item/trip level and nullable `boardingStatus`/`boardedAt` per ticket; optional stop display comes from a server-owned passenger-safe projection. Do not expose `passengerRecordId`; never infer Boarding-open/time/stop from departure time, UUIDs or booking-level status | BE v1.53.0 facade exposes per-ticket lifecycle status but no trip lifecycle, passenger boarding status, boarded timestamp or boarded stop | 2026-08-01 |
| M6.5 | P0 | Continue an existing VNPay session from History List/Detail | BE `main@cd355fff` / `v1.53.0` Passenger History DTOs and existing Mobile payment owners | `IN_PROGRESS` | M3.4, authenticated Passenger History and live VNPay session | Show `Continue payment` only for Ticket `PENDING_PAYMENT` or Parcel `PENDING_PAYMENT`/`PENDING_FINAL_PAYMENT` plus a non-null trusted active redirect returned by BE; reuse the redirect validator/opener, one shared single-flight and foreground/deep-link reconciliation. Never create a second Booking/Parcel/payment, never call Parcel Start-payment to recover an existing URL, and never persist/log/serialize a signed URL in navigation. Only refreshed authenticated BE state can confirm success | v1.53 serializes nullable `paymentRedirectUrl` for exact eligible active sessions. Mobile parses it fail-closed, keeps user-isolated infinite cache/no row requests, renders sibling theme/i18n CTA with active-row busy state, coalesces external opens and refetches only the returning query. TypeScript, scoped zero-warning ESLint, 1,253-key i18n parity and diff check pass; live device payment return remains open | 2026-08-01 |
| M7.1 | P1 | Parcel local photo draft | Expo ImagePicker and parcel draft | `IN_PROGRESS` | Permissions, `expo-image` | Camera/library/preview/remove; local URI never serialized; picker locks during submission | Shared picker and preview remain reused; payload boundary accepts only the remote URL returned by the upload flow. TypeScript/full ESLint/Android export pass; native permission/camera device flow not run | 2026-07-28 |
| M7.2 | P1 | Parcel photo upload | BE `origin/main@d3dc7716` Identity Firebase custom-token purpose `PARCEL_PHOTO` plus Parcel owned-URL validator | `IN_PROGRESS` | Firebase web config, authenticated Passenger, Storage Rules | Prepare/resize/strip metadata on device; request server-owned upload prefix; upload one photo; never construct a path or serialize a device URI; preserve remote URL across ambiguous create retry | Reuses the shared image preparation and serialized Firebase upload services; feature hook is user/session scoped and single-flight; create sends the returned owned URL; detail renders submitted/check-in/delivery photos read-only. TypeScript/full ESLint/4,731-module Android export pass; live Firebase upload remains unverified | 2026-07-28 |
| M7.3 | P0 | Parcel terminal booking flow | BE `origin/main@d3dc7716` / `v1.50.0` locations, stations, available-trip/create/detail and settlement DTOs | `IN_PROGRESS` | Authenticated passenger, a scheduled exact terminal pair with cargo capacity and configured `ParcelRouteFare`, Firebase for optional photo, physical device | Real terminal search; exact dimensions/weight; cancellable pagination; create and payment are separate commands; normal create starts deposit payment once; XL review fails closed; final-payment deadline/amount is surfaced; VNPAY/WALLET reconcile from BE state; every mutation has independent UUID-v4 idempotency and single-flight; terminal failures never render as success | Mobile now consumes the non-legacy create response, starts `/deposit-payment` only after successful create, supports retryable `/final-payment` from detail, displays canonical settlement/evidence fields, and keeps exact BE tracking eligibility. 2026-07-28 TypeScript, full zero-warning ESLint, diff check and 4,731-module Android export pass. No Jest was run per owner request; authenticated payment/photo/device flow remains open | 2026-07-28 |
| M7.4 | P1 | Received Parcel account linking and delivery confirmation | BE `origin/main@d3dc7716` received filter and delivery-token controllers | `BLOCKED_BE` | Safe recipient identity-linking/deep-link contract | A created parcel can appear for the intended Passenger recipient; confirm/reject/undo tokens have an explicit mobile deep-link ownership and UX contract; no guessed account matching | Public create currently persists `RecipientUserId=null` while `GET /parcels/received` filters by `RecipientUserId`; Mobile cannot safely repair recipient linking. Delivery token endpoints exist, but the passenger mobile deep-link/notification handoff is not defined, so no token flow was invented | 2026-07-28 |
| M8.1 | P0 | RAG policy assistant | BE `POST /v1/rag/chat` SSE and feedback | `IN_PROGRESS` | Auth, network | Token/done/error SSE, timeout/abort, conversation and feedback without mock fallback | API/parser/timeout/feedback tests pass; Android auth guard renders; live authenticated SSE not run | 2026-07-15 |
| M8.2 | P1 | Chat booking action | Catalog-backed deterministic intent extraction | `IN_PROGRESS` | M1.2, booking flow | Model output never becomes API payload; prefill only; explicit confirmation before booking/payment | Intent tests pass; Android shortcut opens booking UI without mutation; authenticated prefill/confirmation not run | 2026-07-15 |
| M8.3 | P1 | Chat tracking action | M6 history provider and M5 tracking | `IN_PROGRESS` | M5, M6 | Quick action opens Ticket history and lets user choose an eligible trip | Typed/static implementation passes; eligible-trip device flow not run | 2026-07-15 |
| M9 | P1 | Bus-type filter | Passenger trip-search response | `BLOCKED_BE` | Reliable vehicle metadata | Keep dormant; never hardcode every bus as one type | Current BE response lacks suitable vehicle metadata | 2026-07-15 |
| M10 | P1 | React Native performance and accessibility | `react-native-skills` | `FAILED_VERIFY` | All UI slices, release environment | Virtualized long lists, stable renders, responsive small screen/large font, release frame smoke | Static optimization tests and Android responsive regression pass; release frame gate is `BLOCKED_ENV`, so performance is not certified | 2026-07-15 |
| M11.1 | P0 | Deterministic date/time | Shared formatter contract | `VERIFIED` | None | Stable `DD/MM/YYYY HH:mm`; cached formatter | `format.test.ts`; TypeScript; full Jest and ESLint pass | 2026-07-15 |
| M11.2 | P0 | SecureStore Jest isolation | Production SecureStore adapter | `VERIFIED` | Jest mapper | Reset stored values, calls and mock implementations without production changes | `secureStoreMock.test.ts`; auth logout/cache isolation test; full Jest pass | 2026-07-15 |
| M12 | P0 | Full quality gate | Entire repository | `FAILED_VERIFY` | All modules | Static gates, Expo Doctor, authenticated Android flows, iOS, production release/performance | The 2026-07-23 Expo 54 snapshot passes TypeScript, full zero-warning ESLint, 76/76 Jest suites with 407/407 tests, Expo public config, diff check and a clean 4,542-module Android export. Gradle reaches NDK compile with JDK 21 but the long Windows workspace still exceeds Ninja's 260-character object-path limit. Live booking/payment/history/tracking, regional/Terms eligibility, real Google Maps credentials, key-enabled native/device builds, broader authenticated flows, iOS and production release/frame performance remain unverified, so the repository-wide gate stays open | 2026-07-23 |
| M13.1 | P1 | App icon, native splash and bootstrap loading | Mobile `650bdb4` theme/assets plus `app_logo_placeholder.png` | `IN_PROGRESS` | M13.2 current device artifact | Reuse one canonical logo source; replace default Expo launcher/splash assets for legacy and adaptive Android icons; use one accessible, theme-consistent JS bootstrap screen for font/auth hydration; preserve the separate operation overlay; verify continuous branded cold start without a default/white/blank logo frame | Expo 54 prebuild regenerated launcher/splash resources from the same configured source assets and `AppLaunchScreen.test.tsx` passes. Prior frames belong to the SDK 56 artifact; current SDK 54 clean-install frames are not yet captured | 2026-07-22 |
| M13.2 | P0 | Android internal release APK | Mobile `650bdb4`; Android package `com.vietride.passenger` | `IN_PROGRESS` | Production separately requires M3.5, persistent release key and real Maps key | Produce a release-variant APK labelled internal; verify signature, alignment, package/version/SDK/ABI metadata, clean install/launch and exact deep-link resolution; record the final artifact path, size and SHA-256; never represent debug-key or placeholder-Maps output as Play/production-ready | SDK 54 release variant compiled on JDK 21 and was verified as package `com.vietride.passenger`, version `0.0.1` (1), min/target SDK 24/36, v2-signed with the current debug certificate. Internal artifact `artifacts/vietride-passenger-parcel-flow-20260727-internal.apk`, 125,787,215 bytes, SHA-256 `CBFC63CC40A19F71138389D08BF2B37B0C4CCF393A79AAC9DF242FBD15FD0786`. Clean install/launch and deep-link smoke remain open because no ADB device/AVD was attached; this is not Play/production signing | 2026-07-27 |
| BE1 | P0 | Driver/assistant boarding check-in lifecycle and concurrency safety | BE `main@e13f7475` / `v1.50.1` Boarding controller, scan/tick handlers and shared idempotency pipeline | `BLOCKED_BE` | Explicit policy for pre-boarding preview, `BOARDING`-only versus late `IN_PROGRESS` boarding, intermediate-stop capture and DB concurrency strategy | Manifest/QR resolves exact `ticketCode` (legacy booking code only as fallback) to `passengerRecordId`; Tick uses only `passengerRecordId`, a UUID-v4 idempotency key and an enforced empty body. Central policy verifies assigned crew, allowed trip lifecycle, Booking `CONFIRMED`, Passenger `PENDING` and Ticket `ISSUED`; the local transition to Passenger `BOARDED` plus Ticket `USED` is atomic for two concurrent distinct keys. Define deterministic already-boarded/no-show/not-boardable error precedence; exempt authenticated read-only QR scan with `SkipIdempotency`; do not hold a Booking DB transaction across the Trip HTTP authorization call; define whether/how `boardedAtStop` is captured | Current source correctly uses `passengerRecordId`, but scan/tick ignore trip status; Tick omits Booking/Passenger-state guards, holds its transaction during Trip HTTP, has no row lock/concurrency token, and a normal new-key repeat returns `TICKET_NOT_BOARDABLE` before the documented already-boarded conflict. QR scan unnecessarily requires Redis idempotency; `boardedAtStopId` is always null | 2026-07-30 |
| M14 | P0 | Expo Go SDK 54 compatibility | Expo SDK 54 package matrix and store-distributed Expo Go | `IN_PROGRESS` | Physical Android and iOS Expo Go smoke | Resolve as SDK 54; align React/RN/native packages; emit Expo Go QR explicitly; bundle without the former SDK 56 store mismatch; retain current native capabilities | `expo@54.0.36`, React `19.1.0`, RN `0.81.5`; Expo dependency check passes; Doctor passes all enabled checks; `npm start` now selects `--go`; clean Android export bundles 4,542 modules. Physical Android/iOS Expo Go open remains not run | 2026-07-22 |
| M15.1 | P0 | Firebase avatar contract and UI | BE Identity `CreateFirebaseCustomToken` and `UpdateAvatarResponse` | `IN_PROGRESS` | Firebase project config, authenticated device | Mobile now reads `{ token, purpose, uploadPath }`, appends UUID-v4 filename to server prefix, sends mandatory avatar idempotency header, merges partial avatar response safely, and keeps retry state on ambiguous failure | 2026-07-26 `tsc`, zero-warning ESLint, Expo SDK 54 dependency check, and public config pass; live Firebase/BE upload remains unverified | 2026-07-26 |
| M15.2 | P0 | Google login and phone completion gate | BE `POST /auth/google`, `/users/me/complete-profile`, JWT `hasPhone` claim | `IN_PROGRESS` | Google Console OAuth clients, EAS/dev client, real test user | Native One Tap responses are handled by the current Nitro SDK; backend receives ID token only; no-phone account is gated until phone completion refreshes the VietRide JWT | Login hook/UI, root navigator gate, strict staging/production config and setup guide added; physical Android/iOS OAuth smoke remains open | 2026-07-26 |
| M15.3 | P0 | Expo 54 native Google dependency | Expo SDK 54 matrix and `react-native-nitro-google-signin` docs | `IN_PROGRESS` | Physical-device install and real OAuth smoke | Android native build must complete with JDK 21 and Android SDK; no release/debug signer claims without a produced artifact | 2026-08-01 current working tree compiled successfully from physical `C:\vrb` with JDK 21. `artifacts/VietRide-dev-client.apk` is 232,947,349 bytes, SHA-256 `3011CC97BD3F5AFC3CC68A925FEDB4A0C42780647A407BD859F87B67044A3009`, debuggable, v2 debug-signed, and contains Expo Dev Launcher, `libreanimated.so` and `libworklets.so` for arm64-v8a/armeabi-v7a/x86/x86_64. Manual install, Metro connection and Google OAuth runtime smoke remain open | 2026-08-01 |
| M16 | P0 | App-wide Liquid Glass themes, EN/VI i18n and motion | Mobile `main@15f4a85`, shared Theme/i18n/Motion owners | `IN_PROGRESS` | Physical Android/iOS visual and frame smoke | Every reachable screen/component reacts to persisted Liquid Light/Dark and EN/VI without remounting Navigation on a runtime language change; app-owned validation/error copy is localized and backend prose fails closed; native stack/tab/skeleton motion honors Reduce Motion and stays on UI-thread-safe opacity/transform paths; long lists remain virtualized; no production palette, shadow or formatter owner is duplicated | 2026-08-01 implementation covers Auth, Home, Booking, Parcel, History, Wallet/Profile, Notifications, Tracking and Chatbot plus shared primitives. Static theme/i18n/error audits, 1,231-key locale parity, TypeScript, full zero-warning ESLint, diff check, Expo public config and a 5,138-module Android export pass. Small-screen, large-font, live theme/language switch, Reduce Motion and frame-performance checks on physical Android/iOS remain open | 2026-08-01 |
| M17.1 | P0 | Account-scoped system-tray push | BE `main@cd355fff` / `v1.53.0` Identity device-token endpoints and Notification Firebase worker | `IN_PROGRESS` | Native Firebase Android/iOS client files, BE FCM production env, rebuilt dev clients, physical devices | Use FCM registration tokens only; register after active login/phone completion; serialize registration and token rotation; revoke the exact token before logout/off; show foreground exactly once while the OS owns background display; refresh user-scoped inbox cache; tray tap opens only the authenticated inbox; never trust push data as an arbitrary route/URL | RN Firebase Messaging + Notifee, opt-in native auto-init, secure registration tombstone, refreshed-auth registration, bounded same-operation retries, UUID-v4 idempotency, query invalidation, cold/warm tap queue, real Settings state and setup guide implemented. TypeScript, zero-warning ESLint, i18n, Expo config/dependency checks and the current 5,211-module Android export pass. Autolinking/manifest contain RNFirebase App/Messaging, Notifee, `POST_NOTIFICATIONS`, FCM service, alarm/reboot receivers, the default channel and auto-init=false; a cache-clean assemble exceeded the 20-minute local limit before APK packaging. Native Firebase files are absent, so device delivery remains unverified | 2026-08-01 |
| M17.2 | P1 | Opt-in local daily reminder | Mobile Notifee schedule; no BE daily scheduler/preference contract | `IN_PROGRESS` | Notification OS permission, authenticated active Passenger, rebuilt dev client | Persist one opt-in reminder at 19:00 device-local time; use WorkManager without exact-alarm permission; deterministic ID prevents duplicates; cancel on off/logout; reschedule localized content after language changes | Shared schedule owner and persisted Settings toggle implemented; denied permission opens OS Settings and channel labels resync after language changes. Static gates pass and the merged Android manifest contains Notifee alarm/reboot receivers; physical Android/iOS trigger, reboot persistence and large-font Settings smoke remain open | 2026-08-01 |

## Contract matrix

| Capability | Method/path | Request | Response notes | Mobile rule |
| --- | --- | --- | --- | --- |
| Create booking | `POST /bookings` | Pickup/dropoff, optional `shuttlePickup:{address,latitude,longitude}`, voucher, payment method; `seats: [{seatNumber}]` | WALLET -> `CONFIRMED`; VNPay -> `PENDING_PAYMENT`, `paymentId`, `paymentRedirectUrl`; Shuttle is not echoed | Passenger contact stays local; `Idempotency-Key` is an HTTP header; Shuttle UI/draft is implemented only for the exact origin Station after live Station/Trip eligibility checks |
| Create round trip | `POST /bookings/round-trip` | Independent outbound/return legs with pickup/dropoff/optional Shuttle/seats; group voucher/payment | WALLET confirms both legs; VNPay returns one `BOOKING_GROUP` redirect; Shuttle is not echoed | One shared leg payload builder and single-flight submit; Shuttle drafts remain independent per leg; poll both booking IDs before activating either ticket |
| Station Shuttle capability | `GET /stations/{stationId}` | Exact Station UUID | Active `StationDto` includes coordinates, `supportsShuttle`, `isActive` | Cache by Station ID; require exact response ID and fail closed on 404/mismatch; after v1.33 station merge, refetch trip/station data instead of weakening identity checks |
| Reconcile booking payment | `GET /bookings/{bookingId}` | UUID booking ID | `{bookingId,status}` only | Poll only while focused, foreground and online; only exact `CONFIRMED` is active; `EXPIRED` is payment expiry; later lifecycle states stay inactive and status-neutral |
| Ticket boarding QR resolve | `POST /bookings/trips/{tripId}/boarding/qr-scan` | Exactly one `{ticketCode}` or legacy `{bookingCode}`; current v1.50.1 runtime also requires UUID-v4 `Idempotency-Key` because Booking applies idempotency to every mutation verb | PII-free items with `passengerRecordId`, `ticketId`, `ticketCode`, seat and boarding status | Passenger Ticket detail QR encodes the exact per-seat `ticketCode`, never an internal UUID or booking code. BE1 removes the unnecessary scan idempotency requirement with the existing read-only exemption |
| Ticket boarding tick | `POST /bookings/trips/{tripId}/boarding/passenger/{passengerRecordId}` | Assigned Driver/Assistant JWT, UUID-v4 `Idempotency-Key`, empty body | Marks the Passenger `BOARDED` and Ticket `USED`; returns both IDs/codes and boarded time | `passengerRecordId` is the mutation identifier and never belongs in Passenger UI. BE1 must enforce lifecycle/state/concurrency and deterministic conflict semantics before crew UX is release-ready |
| Shuttle assignment/status | No booking-linked passenger read endpoint | N/A | Create/status responses contain no Shuttle state; authenticated notifications start only at assigned/unfulfilled | Checkout/Ticket may show only that the request was sent and awaits arrangement; History must not invent pending/assigned state |
| Wallet | `GET /wallet` | None | `{userId,balance,currency}` | Cache key includes authenticated user ID |
| Wallet ledger | `GET /wallet/transactions` | `page`, `pageSize` | `CREDIT/DEBIT`, balances, references, note | Map network DTO to a separate UI model; use infinite pagination |
| Top-up | `POST /wallet/top-up` | `{amount,method:'VNPAY'}` | `{topUpRequestId,status,paymentRedirectUrl}` | Validate redirect; do not report success before payment result |
| VNPay browser return | `https://app.vietride.online/payments/return` configured as the BE ReturnUrl | Gateway query is untrusted browser input | BE `410614bdaa5f911edd6d33f86a342e163901b38c` has no deployed bridge/page contract at this path | Keep the ReturnUrl HTTPS; wake Mobile only at exact `vietride://payments/return` or a future verified HTTPS App Link; never derive payment success from query parameters |
| VNPay booking IPN | `POST /payments/vnpay-ipn` through the public v1 API base | Gateway server callback | Server-side payment processing is authoritative | Mobile does not call or emulate IPN; custom return marks authenticated Booking queries stale with no active refetch, while Wallet keeps its existing foreground single-flight reconciliation |
| Tracking realtime | Socket.IO on the public API origin with path `/tracking/socket.io` | Handshake `auth:{token}`; emit `joinTripTracking {tripId}` on every connect/reconnect | `gps:update`, `eta:update`, `trip:statusChanged`; current status event communicates delay, not terminal completion | Never put tokens in URL/query/logs; validate/filter every event by trip/stop; bounded reconnect and fail closed on auth/forbidden/not-found |
| Shuttle tracking | `GET /tracking/shuttle-trips/{shuttleTripId}/latest|eta`; same Socket.IO path; `SHUTTLE_ASSIGNED` notification | Handshake auth; `joinShuttleTracking {shuttleTripId}`; explicit user CTA from validated notification data | Nullable direct latest/ETA payloads; `shuttle:gps:update`, `shuttle:eta:update` | Active only after the authenticated assignment notification supplies the exact UUID; ignore arbitrary `deepLink`, never substitute the main trip ID, and do not invent Shuttle trail/status endpoints |
| Device push registration | `POST` / `DELETE /auth/device-token` | Exact FCM token; platform `ANDROID|IOS`; DELETE body is required despite nullable controller request; current Identity-wide middleware also requires UUID-v4 `Idempotency-Key` on both mutations | Active device registration / 204; BE reclaims rotated token ownership and blacklists invalid FCM tokens | Serialize per-device changes, retain an idempotency key across an ambiguous retry of the same user/token operation, store registration in SecureStore, never log token, revoke before clearing logout credentials, and delete the local FCM token as fail-safe |
| Notification push delivery | Firebase Admin from Notification service | BE-owned title/body and flattened string data | OS renders background/killed notification; foreground arrives to Mobile listener | Do not display again from the background handler; tray tap opens the authenticated inbox and refreshes user-scoped queries; push data never bypasses route validation |
| Tracking latest | `GET /tracking/trips/{tripId}/latest` | UUID trip ID | `{latest: TrackingPoint|null}`; `null` is normal before the first GPS point | Bootstrap once and poll every five seconds only as the disconnected Socket.IO fallback while focused, online, foreground and non-terminal |
| Tracking trail | `GET /tracking/trips/{tripId}/trail` | UUID trip ID; paginated response capped by BE at 100 per request | Ordered GPS points | Initial fetch; refresh at most every five minutes; merge/dedupe/cap newest live points locally |
| Tracking ETA | `GET /tracking/trips/{tripId}/eta?stopId={stopId}` | Valid trip and stop UUIDs | `{eta: TrackingEta|null}`; `null` is normal | Poll every 60 seconds only while lifecycle-active and a destination stop is available; live socket ETA supersedes stale REST values |
| Public trip detail for map lifecycle | `GET /trips/{tripId}` | UUID trip ID | Public trip status, nullable `destinationArrivedAt`, and route stops with coordinates plus `isActive`, `status:PENDING|ARRIVED|SKIPPED`, nullable `actualArrivalTime` | Reuse the existing Trip query owner; poll status at most every 60 seconds while active, stop on a terminal trip, and map only validated coordinates |
| Parcel create | `POST /parcels` | Exact cargo/recipient/trip/payment-method/voucher body plus UUID-v4 `Idempotency-Key`; optional owned Firebase `photoUrl` | Settlement-v2 create facts only; no payment redirect. Normal cargo returns `PENDING_PAYMENT`; derived extra-large cargo returns `PENDING_OPERATOR_REVIEW` | Never read legacy totals/redirect from create. Preserve the created parcel if payment start fails; do not retry create as a new command |
| Parcel deposit payment | `POST /parcels/{parcelId}/deposit-payment` | `{paymentMethod:'VNPAY'|'WALLET'}` plus its own UUID-v4 `Idempotency-Key` | Deposit payment ID/required/paid/due/redirect; zero deposit may activate immediately | Start only for exact `PENDING_PAYMENT`; single-flight; keep key across ambiguous failure; never infer success from redirect |
| Parcel final payment | `POST /parcels/{parcelId}/final-payment` | `{paymentMethod:'VNPAY'|'WALLET'}` plus its own UUID-v4 `Idempotency-Key` | Balance payment ID/required/paid/deadline/redirect | Start only for exact `PENDING_FINAL_PAYMENT`; do not map legacy `PENDING_ADDITIONAL_PAYMENT` to this command |
| Parcel tracking and settlement detail | `GET /parcels/{parcelId}` | UUID parcel ID | Sender/linked-recipient/operator-authorized detail with `tripId`, nullable `dropoffStopId`, canonical estimated/actual cargo, deposit/balance/refund/deadlines, payment IDs and submitted/check-in/delivery photo URLs; no active redirect URL | Reuse one detail query; derive viewer role from authenticated DTO ownership, not navigation; gate sender-only settlement actions; enable live map only for the exact BE tracking allow-list; render evidence read-only |
| RAG chat | `POST /rag/chat` through the v1 client base | `{message,conversationId?}` | SSE `token`, `done`, `error` | No fixture fallback; assistant text never becomes a network action payload |
| RAG feedback | `POST /rag/messages/{assistantMessageId}/feedback` | `{rating:1|-1}` | Standard success envelope | Validate UUID path segment |
| Unified passenger history | `GET /passenger/history` | Required `type=TICKET|PARCEL`; optional exact `status`, inclusive RFC3339 `from`, exclusive RFC3339 `to`; `page`, `pageSize<=100` | Fixed `createdAt DESC, id DESC` page; discriminated Ticket/Parcel items with nullable `paymentRedirectUrl`. Ticket upstream failure is 502, not an empty page; Parcel branch is sent-by-passenger only | One authenticated facade/query owner; type and user remain in the infinite-cache key; use cancellable pagination; strictly accept only nullable trusted HTTPS VNPay URLs; accept safe additive status tokens for display but fail every action closed; never switch failures to fixtures |
| History detail handoff | No individual passenger Ticket-history detail endpoint; existing `GET /parcels/{parcelId}` for Parcel detail | Required selected Ticket facade snapshot or Parcel UUID | Ticket item includes per-seat `ticketCode`, seat, lifecycle/paid amount, route summary and an eligible nullable active redirect, but no payment method, bus, stop address/ID, trip lifecycle or boarding timestamp | Reuse the existing Ticket view-model and Parcel detail query; strip the signed URL before Ticket navigation and pass only Parcel UUID; never read checkout Zustand state or misuse status-only `GET /bookings/{id}` |
| History payment resume | v1.53 `GET /passenger/history` nullable `paymentRedirectUrl` | Existing authenticated Ticket/Parcel History query; exact server pending status | URL is non-null only for a resumable active Ticket VNPay session or the exact active Parcel deposit/final session; otherwise null | Render a sibling List CTA only after URL and domain-owned status allow-lists both pass; use the shared trusted opener/single-flight and one foreground/deep-link refresh. Never create/restart payment or serialize the URL in navigation |
| Parcel photo upload | `POST /firebase/custom-token` with purpose `PARCEL_PHOTO`, then Firebase Storage | Authenticated purpose request; object name appended under the server-returned `uploadPath` | Owned download URL under `parcels/{userId}/`; Parcel create validates bucket and owner prefix | Reuse shared preparation/upload services; never build the prefix client-side; local URI never enters the Parcel request |

## Product-scope restoration matrix

| Removed or degraded capability | Disposition | Replacement / reason |
| --- | --- | --- |
| Password-reset mock API | `REPLACE_DATA_SOURCE` | Real Identity forgot/reset controllers; no duplicate mock client |
| Popular route cards/screen | `RESTORE` | Catalog-resolved locations; no fake fare or duration |
| Recent search cards | `RESTORE` | Versioned AsyncStorage provider scoped to user/guest |
| Monolithic booking mock data | `REPLACE_DATA_SOURCE` | Split into live API, local product data, or explicit demo provider |
| Wallet and Top-up | `REPLACE_DATA_SOURCE` | Real Payment APIs with React Query and lifecycle-safe mutation control |
| Withdraw, Saved Payments, Add Card | `KEEP_SHELL` | Visible/reachable labelled shells; no unsupported financial input |
| Booking history fixtures | `RESTORE` | Preserve the explicit development/staging-demo provider for missing-capability demos; production and normal authenticated History use the live passenger-history facade and never fall back to fixtures on failure |
| Digital Ticket history flow | `REPLACE_DATA_SOURCE` | Live selected Ticket facade snapshot, with guarded demo-detail fallback only for legacy demo routes; never misuse the status-only endpoint or fabricate fields the facade does not return |
| Sent Ticket/Parcel history | `RESTORE` | One live facade with required type-specific requests, infinite pagination, server filters and typed branch models; sent Parcel history remains distinct from Home received parcels |
| Tracking/MockMap | `REPLACE_DATA_SOURCE` | Shared Ticket/Parcel Socket.IO tracking with lifecycle-safe REST fallback and native Google map; do not restore `MockMapView` |
| Home booking-voucher promotions | `REPLACE_DATA_SOURCE` | Existing Booking promotions API/query keys |
| Editorial news/product promos | `BLOCKED_BE` | Distinct from booking vouchers; wait for a real editorial/campaign contract |
| Home recent shipments | `REPLACE_DATA_SOURCE` | Existing received-parcels API/hook |
| Referral rewards/invite card | `BLOCKED_BE` | No referral/reward contract; hard-coded coins/cash rewards are not restored |
| Home Wallet card | `RESTORE` | Real Wallet states and typed navigation |
| Parcel photo components | `RESTORE` | Shared native picker/local preview plus authenticated Passenger Firebase upload; submitted and operational evidence use a read-only `expo-image` gallery |
| Bus-type filter | `BLOCKED_BE` | Dormant until passenger trip metadata supports it |
| RAG policy chatbot | `RESTORE` | Hardened BE SSE/feedback integration |
| Chat booking/tracking shortcuts | `RESTORE` | Deterministic prefill and explicit user-controlled navigation |
| Per-ticket Shuttle request | `RESTORE` | Theme-consistent optional origin-Station flow backed by current Booking/Station/Trip contracts; precise coordinates stay memory-only |
| Shuttle intent/history/assignment | `BLOCKED_BE` | Create/status responses do not expose Shuttle state; post-assignment notification/tracking cannot reconstruct pending intent by booking |
| Passenger Shuttle live map | `RESTORE` | Exact `shuttleTripId` is now available after assignment through authenticated `SHUTTLE_ASSIGNED`; reuse the single live tracking panel/map owner and keep pre-assignment History state unavailable |
| System-tray notification | `REPLACE_DATA_SOURCE` | Reuse BE FCM/device-token pipeline through one native coordinator; REST inbox remains the source of display/read state |
| Daily notification | `KEEP_SHELL` | Opt-in generic local 19:00 reminder until BE defines personalized digest preferences, timezone and scheduler semantics |
| Payment return deep link | `RESTORE` | Exact custom URI plus HTTPS App Link intake; query data is only a wake signal and authenticated BE state remains authoritative |
| Default Expo launcher/splash/loading | `REPLACE_DATA_SOURCE` | Reuse `app_logo_placeholder.png` through shared assets, generated native Android icon/splash resources and the shared bootstrap loading screen |

## Implementation rules

1. Never use blanket `git reset`, checkout or revert against user/Claude work.
2. Never invent an endpoint, identifier, response field, fare, duration or inventory value.
3. A missing BE capability remains local product behavior, a labelled shell or `BLOCKED_BE`; it is not deleted.
4. API failure never switches silently to fixture data.
5. Reuse API clients, auth refresh, error envelope, query keys and hooks before creating another abstraction.
6. Private cache keys include authenticated user ID and are cleared on session changes.
7. Large lists use virtualization, memoized rows, primitive props and stable callbacks.
8. Payment and booking submissions are single-flight and idempotency-safe across ambiguous failures.
9. No row becomes `VERIFIED` without evidence matching its full acceptance criteria.
10. No commit or push is created unless explicitly requested.

## Final acceptance gate

- [x] `npx.cmd tsc --noEmit`
- [x] `npm.cmd test -- --runInBand` - PASS (76/76 suites, 407/407 tests on 2026-07-23)
- [x] `npx.cmd eslint . --max-warnings=0`
- [x] `npx.cmd expo config --type public`
- [x] `git diff --check`
- [x] Android debug x86_64 native build and unauthenticated/guest smoke
- [x] Windows stable Metro startup, Android bundle and authenticated Home/API startup smoke
- [x] Android custom payment-return resolution/security smoke and branded native-to-JS cold launch
- [x] Previous Android tracking Maps manifest merge and `arm64-v8a` debug native build
- [ ] Current key-enabled Android/iOS native Maps compile and physical-device smoke after regional eligibility is cleared
- [ ] Live one-way and round-trip Shuttle booking submit/confirmation
- [x] Expo Doctor enabled checks - PASS (17/17); tracked-native sync check intentionally disabled with current clean Android prebuild evidence
- [x] Expo SDK 54 dependency matrix and clean Android export
- [x] Current Expo 54 Android development-client native assemble with Nitro, Reanimated and Worklets included
- [x] App-owned EN/VI key parity and static runtime-theme/error-surface audit (1,231 locale keys on 2026-08-01)
- [ ] Physical Android/iOS Expo Go launch on the current SDK 54 snapshot
- [ ] Physical Android/iOS Liquid Light/Dark, EN/VI hot switch, Reduce Motion, small-screen, large-font and frame-performance smoke
- [ ] Authenticated Android: 100+ ledger, top-up return, ticket detail, tracking and live RAG
- [ ] iOS: safe areas, large text, keyboard, photo permission and map fallback - `BLOCKED_ENV`
- [ ] Android internal release APK: assemble/signature/package metadata complete; clean install/cold-start, branded visual QA and exact deep-link resolution smoke remain open without an attached device
- [ ] Android production/Play release and frame gate - `IN_PROGRESS` (persistent production signing key and real Maps key absent); verified HTTPS App Link remains `BLOCKED_BE` until the domain route and `assetlinks.json` are deployed

## Progress log

### 2026-08-01 - BE v1.53 Tracking audit and native notifications

- Re-pinned and read-only audited BE `main@cd355fff` (`v1.53.0`). Passenger trip/Shuttle REST and Socket.IO wire contracts remain compatible; the latest Tracking change only deduplicates producer GPS writes. Booking/History still does not expose `shuttleTripId`, while authenticated `SHUTTLE_ASSIGNED` notification data does.
- Extended the existing Tracking API, realtime owner, lifecycle hook, panel and native map rather than creating a second map stack. Shuttle tracking is reachable only from an allow-listed notification with validated UUID data; arbitrary server `deepLink` values are never executed.
- Integrated RNFirebase Messaging and Notifee through one app-level coordinator. Registration uses the existing authenticated API client, UUID-v4 idempotency, serialized token rotation, bounded retry and a SecureStore cleanup tombstone; foreground display occurs once and the OS remains the background/killed display owner.
- Added real Settings toggles for account-scoped push and an opt-in local 19:00 reminder, including denied-permission recovery, localized Android channels and foreground reconciliation. FCM auto-init is disabled until permission/opt-in is accepted.
- Current TypeScript, full zero-warning ESLint, 1,253-key EN/VI parity, diff check, Expo dependency/config checks, 5,211-module Android export and native manifest assertions pass. No Jest was added or run per owner request. Native Firebase client files, a rebuilt dev client and physical-device Firebase/Maps sessions remain required before M5.1/M17 can become `VERIFIED`.

### 2026-08-01 - BE v1.53 Passenger History payment resume

- Re-audited BE `main@cd355fff` (`v1.53.0`) without editing it. The unified Ticket/Parcel facade now always serializes nullable `paymentRedirectUrl`, populated only for the exact eligible pending state and active VNPay session.
- Extended the existing strict Passenger History schema/type instead of adding another client or hook. Supplied URLs must be valid trusted HTTPS VNPay URLs; missing fields default to null only for rolling deployment compatibility.
- Added one sibling `Continue payment` action to memoized Ticket/Parcel rows. Domain-owned status policies remain the independent eligibility gate, the active row exposes a busy state, external launches are single-flight, and signed URLs are stripped from navigation state.
- Reused the generic payment-return gate and trusted redirect opener across Wallet, Booking, Parcel and History. Closing VNPay and returning foreground consumes one armed round trip and refetches only its initiating History query; the global deep-link handler marks user-scoped History stale without issuing a competing refetch.
- Full TypeScript, scoped zero-warning ESLint, `check:i18n` with 1,253 EN/VI keys and scoped `git diff --check` pass. No Jest was added or run per owner request; physical Ticket/Parcel VNPay resume remains required before `M6.5` can become `VERIFIED`.

### 2026-08-01 - App-wide Liquid Glass, EN/VI and motion migration

- Preserved the two existing Liquid Glass variants as the single runtime theme source, persisted theme and language preferences, and kept Navigation mounted during post-bootstrap language changes so local screen state is not discarded or flashed.
- Android navigation-bar styling now lazy-loads its Expo native module and fails safely when a Metro update reaches an older development client/APK. Expo SDK 54 resolves `expo-navigation-bar@5.0.10` through native autolinking; rebuilding the client enables the system-button styling, while an older client no longer blocks app bootstrap.
- Rebuilt the canonical Android development client from a clean physical short-path mirror with JDK 21 after the installed binary reported missing NavigationBar/Worklets native code. Verified the debug/dev-launcher manifest, v2 debug signature and per-ABI Reanimated/Worklets libraries, then replaced only `artifacts/VietRide-dev-client.apk`; physical install and Metro runtime smoke remain owner-side because no connected ADB target was requested.
- Restored the Liquid Glass theme file and floating bottom-navigation geometry to visual source commit `424c2476d96e4b4f04756b5c7be0cfcc94d3725a`: a 76px absolute pill with safe-area bottom offset, the original light/dark halo, borders, shadows and compact scale motion. Kept the current Reanimated, Reduce Motion, Expo Image and i18n/accessibility implementation, and centralized the repeated `bar height + safe inset + content gap` calculation in `useFloatingTabBarContentInset`. Full TypeScript, zero-warning ESLint, 1,231-key locale parity, diff check and a 5,140-module Android export pass; physical visual QA remains open.
- Migrated reachable Auth, Home, Booking, Parcel, History, Wallet/Profile, Notification, Tracking and Chatbot surfaces plus shared primitives to theme tokens and app-owned EN/VI copy. Backend validation prose now fails closed through one localized API-error path instead of leaking arbitrary server/native messages.
- Reused cached locale-aware formatters, shared presentation/status owners and existing API/query infrastructure. Removed duplicate formatter/shadow ownership and retained fixed black/white only where QR scanning or native-map contrast requires it.
- Moved tab/header/skeleton motion to Reanimated UI-thread-safe opacity/transform paths, honored platform Reduce Motion, and retained FlashList/memoized rows/stable callbacks for History, Wallet and other hot list paths.
- `npm.cmd run check:i18n` passes with 1,231 EN/VI keys; full TypeScript, zero-warning ESLint, Expo public config, `git diff --check` and a clean 5,138-module Android export pass. No Jest was run per owner request. Physical Android/iOS layout, accessibility and frame-performance smoke remains open, so M16 is intentionally `IN_PROGRESS` rather than overstated as verified.

### 2026-07-30 - Re-audit of Boarding and Passenger History UX plan

- Re-pinned the working source to Mobile `afb7ea8` and BE `e13f7475` / `v1.50.1`, then re-read the current Boarding handlers, idempotency pipeline, passenger-history facade and both existing detail screens. No BE or Mobile runtime code was changed in this plan-only audit.
- Confirmed the operational Ticket flow is `ticketCode` (legacy booking code fallback) -> QR resolve -> `passengerRecordId` -> Tick. `ticketId` is response metadata only. Strengthened BE1 for missing trip/Booking/Passenger guards, deterministic conflicts, distinct-key concurrency, empty-body enforcement, scan idempotency exemption, transaction-over-HTTP and boarded-stop policy.
- Split History tracking into its data/query/navigation foundation, shared List presentation, Ticket Detail, sent-Parcel Detail, currently available per-ticket `USED` display, BE-blocked live boarding facts and BE-blocked active VNPay resume. This keeps aesthetic work independent from missing BE capabilities.
- Corrected clean-code ownership: Booking and Parcel each own their semantic vocabulary; History composes those owners instead of creating a cross-domain status mapper. Existing payment/tracking policies, view models, query hooks, FlashList, theme tokens and cached formatters remain the reuse targets.
- Added the missing operational QR acceptance. Current Ticket Detail joins codes behind a Ticket icon, and Parcel Detail shows a decorative QR icon; release-ready detail must render one scannable exact server code per Ticket/Parcel without encoding internal IDs or PII.
- Recorded that Passenger Parcel History is sent-only, while received parcels remain a separate capability. Parcel Detail actions must derive sender/recipient role from authenticated response ownership, not from a navigation flag.

### 2026-07-28 - Parcel settlement v2, owned photos and history compatibility

- Re-audited BE `origin/main@d3dc7716` (`v1.50.0`). Parcel creation no longer starts payment or returns a redirect; deposit and final settlement are independent idempotent commands. The detail DTO now owns estimated/actual cargo, deposit/balance/refund/deadline fields and submitted/check-in/delivery photo URLs.
- Reused the existing API client, auth/session epoch, React Query, idempotency tracker, payment redirect validator, Wallet cache and Firebase image services. One generic Parcel submission coordinator now provides per-command single-flight behavior, retains UUID-v4 keys only across ambiguous failures and resets on success or definitive client rejection.
- Updated checkout to upload the optional passenger-owned photo first, create exactly one parcel, then start its deposit only for `PENDING_PAYMENT`. A saved parcel is never lost or recreated when payment start fails; detail supplies an explicit retry for deposit or exact `PENDING_FINAL_PAYMENT`, while operator-review and unknown legacy states fail closed.
- Updated strict History parsing and detail presentation for `RESERVED`, `CHECKED_IN`, `PENDING_FINAL_PAYMENT` and `READY_TO_LOAD`. The delivery code and live-map eligibility remain separate: tracking still uses only the exact BE authorization allow-list and starts after loading.
- Added canonical settlement progress, final-payment deadline, Wallet/VNPay selection reused from checkout and a read-only `expo-image` evidence gallery. No passenger evidence-upload purpose or sender-cancel endpoint was invented.
- No Jest suite was run or added per owner request. Full TypeScript, full zero-warning ESLint, `git diff --check` and a clean 4,731-module Android development export pass. Live Firebase upload, deposit/final payment callbacks, authenticated History and physical-device layout/performance remain required before the Parcel rows can become `VERIFIED`.

### 2026-07-27 - Parcel terminal booking flow recovery

- Re-audited the latest Parcel, Location and Trip BE code. Parcel availability is keyed by the exact origin/destination station UUIDs, Vietnam-local departure date, dimensions, weight and size category; a scheduled Ticket trip alone is insufficient unless cargo capacity and a matching route/size parcel fare are configured.
- Removed mock route/district behavior from the reachable flow while preserving the district capability as a fail-closed shell. The Home and Parcel headers now use selected live locations/stations, and Vietnamese search normalizes both accented and unaccented `Lâm Đồng` input.
- Rebuilt the four-step flow around the existing API client, React Query and Zustand owners: dependent station state clears on location changes, station requests are cancellable, trip pages are loaded incrementally, thirty departure days are available, and empty results explain the exact terminal-pair requirements with terminal/date recovery actions.
- Replaced the lag-prone JS drag loop with the Expo-compatible native community slider. Invalid text drafts now block navigation instead of silently reusing stale measurements; exact dimensions automatically promote Small to Medium/Large fare tiers and oversized unsupported dimensions fail clearly.
- Hardened final submission and payment lifecycle: every prior step is revalidated, recipient phone uses the BE's VN normalization contract, availability and Wallet caches refresh after create, VNPay status reconciles on return, a trusted redirect can be reopened only within the transient checkout session, and cancelled/rejected/expired parcels never show an active delivery code or success state.
- No Jest suite was run or added per owner request. Full TypeScript, targeted zero-warning ESLint, `git diff --check`, a 4,609-module Android export, a JDK 21 universal debug build and a standalone release-variant build passed. The internal APK is v2-signed with the current debug certificate and recorded under `artifacts/`; no ADB device or local AVD was attached, so authenticated physical-device HCM-to-Lâm Đồng search/create, cold-start/deep-link and gesture UX remain required before `M7.3` can become `VERIFIED`.

### 2026-07-23 - Google Maps tracking UX, native setup and compliance hardening

- Re-audited the current Google Maps Platform Android/iOS setup, renderer, marker, polyline, camera, gestures, controls, API-key security, attribution and Terms guidance. Kept the architecture to Maps SDK for Android/iOS only; live GPS, trail, stops and ETA continue to use the existing authenticated VietRide Tracking REST/Socket.IO owner.
- Improved the shared Ticket/Parcel native map without adding another API, hook or provider: the initial viewport waits for native readiness, pan and pinch reliably pause follow mode, the explicit Follow bus action resumes bounded camera updates, and a two-layer native polyline stays legible over the basemap. Static markers keep view tracking disabled and nonessential map layers/controls remain off.
- Hardened native configuration for eligible builds: Android pins Maps SDK 19.2.0, explicitly requests the latest renderer and registers usage attribution; iOS initializes the Google provider once, registers attribution and adds the documented URL schemes. Platform keys remain native-only and stale configuration is removed when disabled.
- Added a fail-closed production eligibility gate because Google's current Prohibited Territories list includes Vietnam. The default does not embed or initialize Google Maps in production; enabling it requires documented Google/legal confirmation and is not a Terms bypass.
- TypeScript, full zero-warning ESLint, `git diff --check`, Expo config, all 76 Jest suites/407 tests and a clean 4,542-module Android export pass. Gradle 8.14.3 reaches NDK compile with the compatible Android Studio JDK 21, then the long Windows workspace hits the known Ninja 260-character path limit; a short physical-path key-enabled build remains open. Real keys, authenticated trips, physical Android/iOS smoke and regional eligibility remain open, so M5 stays `IN_PROGRESS`.

### 2026-07-22 - Expo SDK 54 compatibility downgrade

- Downgraded the runtime coherently from Expo 56/RN 0.85/React 19.2 to Expo `54.0.36`/RN `0.81.5`/React `19.1.0`, including every Expo-managed native package, RN tooling, TypeScript and Jest preset. `npm start` and the stable launcher now select Expo Go explicitly.
- Rebuilt the lockfile from a clean dependency tree and regenerated Android with the SDK 54 Gradle/native template. Payment deep links, backup restrictions, permission removals and branding configuration remain represented; no feature, mock provider or product module was retired.
- Kept Expo Go's required `react-native-maps@1.20.1` and replaced the unavailable SDK 56-only maps plugin import with one shared SDK 54-compatible config plugin. Native Maps keys remain outside JavaScript and invalid key formats fail closed.
- Consolidated duplicate Expo configuration into `app.config.js`, eliminating config drift. Expo dependency validation passes; Doctor passes all 17 enabled checks; TypeScript, zero-warning ESLint, `git diff --check` and full Jest pass (76 suites, 405 tests).
- A clean Android export completed 4,542 modules in about 42 seconds and exited successfully; Metro's final displayed `99.9%` line is progress rendering, not a stalled process. The current SDK 54 physical Expo Go launch and native APK build remain open.
- Production dependency audit has no high/critical findings. React Native CLI was patched to compatible `20.2.0`, reducing moderate findings from 19 to 12; remaining advisories are in Expo 54 build tooling and npm's forced fix would upgrade to Expo 57, so it was intentionally not applied.

### 2026-07-22 - Unified passenger history and BE v1.40 contract sync

- Re-audited BE `origin/main@fcccf454` (`v1.40.0`). Passenger History is now the authenticated facade `GET /v1/passenger/history`; `type` is required and accepts only `TICKET` or `PARCEL`, with optional exact status/date filters and fixed descending infinite pagination. The Parcel branch contains sent parcels, while the existing received-parcels endpoint remains a separate Home capability.
- Added one runtime-validated passenger-history API/query owner using the existing API client, auth refresh, error envelope and React Query infrastructure. Cache keys are isolated by user/type/filter, inactive tabs are lazy, requests are cancellable, retries are bounded and remote errors never switch to fixtures.
- Rebuilt History on memoized FlashList rows with server-backed Ticket filters, pagination, refresh/stale/error/empty states and theme-consistent responsive cards. Ticket and Track presses are separate; Parcel reuses the existing detail flow by the facade parcel UUID.
- Digital Ticket now accepts the selected serializable Ticket snapshot and does not read checkout Zustand state for history routes. Fields absent from the facade (payment method, bus type, stop address/ID) are omitted rather than fabricated; legacy explicit-demo detail remains preserved without running when a live snapshot exists.
- Synced additive v1.40 Trip detail fields (`destinationArrivedAt`, stop `status`, `actualArrivalTime`, `isActive`) through the existing mapper. Current TypeScript, full zero-warning ESLint, Expo config and diff check pass. No new tests were added and the full Jest gate was intentionally deferred per owner request; authenticated device pagination/detail remains open, so M6 stays `IN_PROGRESS`.

### 2026-07-20 - Realtime Ticket/Parcel tracking and native Google Maps

- Re-audited BE `origin/main@16ea6000` (`v1.36.0`). Realtime tracking uses Socket.IO on the public API origin at `/tracking/socket.io`, joins on every connection, and consumes validated trip-scoped GPS/ETA/delay events; authenticated REST latest/trail/ETA remains the lifecycle-aware bootstrap and fallback.
- Added one shared live-tracking panel for Ticket and Parcel instead of parallel feature implementations. Ticket keeps the checkout/history `tripId`; Parcel reuses its existing detail hook for `tripId` and `dropoffStopId`. The public Trip query supplies validated route-stop coordinates and terminal status without relying on the missing passenger `booking/me` contract.
- Added a native Google map with a capped/deduplicated travelled trail, stable bus/stop markers, follow/recenter behavior and safe unavailable state. No Directions/Routes request, background location permission, untrusted key in JavaScript, or second API/query owner was introduced.
- Added native-only Android/iOS Maps key configuration, production fail-closed checks and `docs/GOOGLE_MAPS_SETUP.md`. The Google Cloud project configuration remains unverified; separate package/bundle-restricted keys and a real authenticated trip/map smoke are required before M5 can become `VERIFIED`.
- Current snapshot passes TypeScript, 76/76 Jest suites (402/402 tests), zero-warning ESLint, Expo public config, Android Maps manifest merge and an `arm64-v8a` debug APK build. The universal build from this long Windows workspace still hits the native CMake path-length limit, so it is not claimed as a current universal artifact.

### 2026-07-18 - Payment return, branded launch and Android internal release verification

- Re-audited Mobile `650bdb4` against BE `410614bdaa5f911edd6d33f86a342e163901b38c` (`v1.34.2`). The BE requires an HTTPS VNPay ReturnUrl and exposes the server IPN; `app.vietride.online` currently has no DNS/HTTPS return bridge, while apex `assetlinks.json` identifies the driver package with a debug fingerprint rather than the passenger app.
- Verified exact custom-scheme payment return handling. Mobile rejects spoofed/ambiguous URLs, ignores gateway query data as a result, deduplicates cold/warm delivery and marks only the authenticated user's Booking queries stale with `refetchType: 'none'`; Wallet continues to own foreground single-flight reconciliation.
- Replaced default Expo branding with the existing placeholder logo across app config, legacy/adaptive Android launcher resources, native splash resources and the shared font/auth bootstrap screen. The static `defaultSource` with zero fade passes focused coverage; final clean-install frames 250-1050 ms show native and JS logos, dark system icons and no white or empty-logo frame.
- Final snapshot passes TypeScript, full Jest (69/69 suites, 361/361 tests in 35.43 s), zero-warning ESLint, Expo config and diff check. The final APK cold-launched with `Status: ok`/`MainActivity`; exact `vietride://payments/return` resolved and `vietride://payments/return/evil` did not.
- The polished universal internal APK passed alignment, apksigner verification (`v2=true`, `v3=false`), package/version/SDK/ABI inspection, clean installation, cold launch and exact/hostile deep-link smoke. Final artifact: `artifacts/VietRide-0.0.1-internal-release.apk`, 59,932,571 bytes, SHA-256 `9C765608E938CDA14954C971AA9CB984AA8C3367DA9DAA014816FFA356C65780`. Production remains open because only a debug signer and placeholder Maps key are available.
- Deletion of the default `ic_launcher_background.xml` is intentional: `vietride_splash.xml` and branded launcher resources replace it, so no product capability was removed.
- BE handoff remains fixed: `VNPAY_RETURN_URL=https://app.vietride.online/payments/return`, booking IPN `https://api.vietride.online/v1/payments/vnpay-ipn`, mobile fallback `vietride://payments/return`, Android package `com.vietride.passenger`.

### 2026-07-17 - Shuttle booking and Windows Metro recovery against BE v1.33.0

- Re-audited the latest fetched BE ref `origin/main@0a1bd9f` (`v1.33.0`) without changing the BE worktree. One-way and round-trip Shuttle DTOs remain independent optional leg fields; exact origin Station eligibility, `SCHEDULED` status and strict T-30 cutoff remain authoritative.
- Added the theme-consistent Shuttle option to the existing pickup step, a responsive keyboard-safe address/location sheet, independent outbound/return memory-only drafts, one shared leg payload builder and pending-assignment summaries in Checkout, Payment and Digital Ticket.
- Reused the existing API/auth/query infrastructure and shared location implementation. Station detail is prefetched, precise coordinates are never persisted/logged, local Station binding is stripped from payloads, and incomplete Trip Detail now fails closed.
- Diagnosed the Windows 99% stall as Expo SDK 56 opening roughly one fallback `fs.watch` handle for each of 12,613 directories before binding Metro. Added `npm run start:stable`, which uses the supported CI/no-watch path and LAN binding without deleting caches or dependencies.
- `npm run start:stable` bound `0.0.0.0:8081`; Android bundled 4,587 modules and Expo Go reached authenticated Home with successful live GET responses. Fast Refresh is intentionally unavailable in this fallback mode.
- Passed TypeScript, full Jest (66 suites/347 tests), zero-warning ESLint, Expo config and `git diff --check`; no tracked file was deleted. Live Shuttle mutation, iOS, current native rebuild and release performance remain unverified, so M1.4/M12 are not marked `VERIFIED`.

### 2026-07-15 - Booking flow synchronized with BE v1.30.0

- Re-audited local BE HEAD `5be88a5` (`v1.30.0`). The public create DTOs are unchanged from `e2b85a1`; v1.30 adds internal payment context/invoice settlement and later booking lifecycle completion.
- Preserved the exact one-way/round-trip payloads, station/stop identity and local-only passenger contact; added user-scoped `GET /bookings/{bookingId}` reconciliation for VNPay.
- WALLET confirmation now goes directly to the ticket; VNPay stays pending until every one-way/round-trip booking ID is exactly `CONFIRMED`. `EXPIRED`, `COMPLETED`, `CANCELLED`, `NO_SHOW`, `PARTIAL_NO_SHOW`, `REFUNDED` and `DISRUPTED` cannot render as an active ticket.
- Added bounded foreground/focus/online polling, transient retry, abort on focus loss, fatal 403/404 fail-closed behavior, user/booking cache isolation and a shared AppState hook reused by Booking and Tracking.
- Added single-flight create/navigation/redirect handling, deterministic idempotency retry policy (retain on network/timeout/408/5xx, rotate after definitive rejection), checkout-step fixes and immutable submitted payment-method display.
- Passed `npx.cmd tsc --noEmit`, full Jest (60 suites/304 tests), zero-warning ESLint and `git diff --check`; no tracked file was deleted. `LIVE_BE` checkout remains `NOT_RUN` and M1.2 remains `IN_PROGRESS`.

### 2026-07-15 - Corrective recovery implementation and verification

- Re-audited the Claude batch against BE `e2b85a1`; corrected booking, wallet, tracking and RAG contracts without blanket revert.
- Restored product scope through live adapters, local storage, labelled shells or explicit demo providers; no tracked module was deleted.
- Completed user-scoped Wallet/Top-up infrastructure, provider-safe History/Digital Ticket, Tracking lifecycle, Home sections, parcel local photos and deterministic chatbot actions.
- Added auth logout cache-isolation coverage and a shared responsive auth footer after Android small-screen/large-font smoke found overflow.
- Passed TypeScript, 55 Jest suites/256 tests, zero-warning ESLint, Expo config, diff check and Android debug native build.
- Recorded remaining blockers honestly: no authenticated live test account, no iOS environment, Expo Doctor external network denial, no real Maps key and Windows release CMake path limit.
